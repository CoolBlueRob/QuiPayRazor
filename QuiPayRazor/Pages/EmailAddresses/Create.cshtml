@page
@model QuiPayRazor.Pages.EmailAddresses.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>EmailAddress</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EmailAddress.AddressType" class="control-label"></label>
                <select asp-for="EmailAddress.AddressType" class="form-control"></select>
                <span asp-validation-for="EmailAddress.AddressType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmailAddress.Email" class="control-label"></label>
                <input asp-for="EmailAddress.Email" class="form-control" />
                <span asp-validation-for="EmailAddress.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmailAddress.MemberIdentityID" class="control-label"></label>
                <select asp-for="EmailAddress.MemberIdentityID" class ="form-control" asp-items="ViewBag.MemberIdentityID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
