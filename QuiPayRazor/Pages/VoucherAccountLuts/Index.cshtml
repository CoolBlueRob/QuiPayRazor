@page
@model QuiPayRazor.Pages.VoucherAccountLuts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VoucherAccountLut[0].BankNoteAccountLutState)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VoucherAccountLut[0].BankNote)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VoucherAccountLut[0].Account)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.VoucherAccountLut) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BankNoteAccountLutState)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BankNote.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
